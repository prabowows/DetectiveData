# Supabase project credentials.
# Detective Data runs fully in "local mode" (browser storage) if these are left blank,
# so you can try the game immediately without setting up Supabase first.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
